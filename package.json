{
  "name": "@devmule/dynamic-buffer",
  "version": "1.0.0",
  "description": "",
  "keywords": [
    "serialization",
    "buffer",
    "dynamic"
  ],
  "author": "devmule",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc --watch --noEmit",
    "prebuild": "rimraf ./dist",
    "build": "tsc"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "typescript": "^4.7.4",
    "rimraf": "^3.0.2"
  }
}
